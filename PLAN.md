# Eclipse Roguelike — Plan Index

A single-player, browser-only roguelike inspired by Eclipse (the board game):
the player counter-builds a small fleet against telegraphed enemies across a
branching run map. React + TypeScript + Vite, no backend, Vitest.

## How the plan is organized

Specs live in `plans/`, **one file per iteration**. This file is just the
index — full specs, tests, milestones, and implementation notes live in the
iteration files.

Rules for working with these files:

- **Implementers:** read only the iteration file you are implementing (plus
  the codebase — the code and its tests, not the older plan files, are the
  source of truth for current behavior). Record your status notes,
  deviations, and verification results **in that same iteration file**, in
  the established style (status blockquote at the top, notes at the end).
- **Authority:** later iterations override earlier ones wherever they
  conflict. The older files are kept as history and rationale, not as
  current rules.
- **Planning** happens in chat and lands as a new `plans/iteration-N.md`;
  ideas that were discussed but deferred or rejected are in
  `plans/parking-lot.md` (including explicit "decided against" entries — do
  not relitigate those without new evidence).

## Status

| Iteration | Theme | Status |
|---|---|---|
| [1](plans/iteration-1.md) | MVP: core rules, resolver, forecast, 9-fight gauntlet | implemented |
| [2](plans/iteration-2.md) | Fleet building, credits + trade station | implemented |
| [3](plans/iteration-3.md) | Branching map, events, reaction cards, persistent damage | implemented |
| [4](plans/iteration-4.md) | Reward screen, ship upgrades, enemy escalation + intel | implemented |
| [5](plans/iteration-5.md) | Combat & fleet depth: retreat, exotic weapons, taunt/Bastion, boss variety | implemented |
| [6](plans/iteration-6.md) | Strategic layer: fog of war, info broker, quests, commanders | implemented |
| [7](plans/iteration-7.md) | Arsenal & tactics: new weapons/defenses, active parts, card + intel-currency reworks, shop rework | implemented |
| [8](plans/iteration-8.md) | The long war: two-act map, safe opener, veterancy scaling, act-2 roster, final bosses, Dreadnought (+ addendum: Jink, job stakes, chaff, upgrade cap) | implemented |
| [9](plans/iteration-9.md) | Persistence (localStorage saves + full-run determinism), mixed formations + targeting doctrine, Cruiser frame | implemented |
| [10](plans/iteration-10.md) | Space-game visual overhaul: design system, starchart map, ship silhouettes, animated combat theater (presentation only) | implemented (partial — see iteration-10.md deviations) |
| [11](plans/iteration-11.md) | Tempo & teeth: auto-heal interlude, initiative-as-evasion rework, act-1-late/act-2 difficulty pass tuned via the re-armed balance script | **closed (2026-08-12), loose end resolved** — the I8-addendum items and the auto-heal interlude landed; the evasion rework was never built as specced and is superseded by [17](plans/iteration-17.md)'s Outspeed mechanic; the difficulty-pass goal continues in [55](plans/iteration-55.md) |
| [12](plans/iteration-12.md) | Coordinate layer & decision support: starchart edges/trail, combat-theater fx, hit-chance matrix, forecast restore + delta preview | implemented |
| [13](plans/iteration-13.md) | Dice on the table: weapons/rolls as dice, shared stat bar, click-to-focus priority targeting, forecast removed, combat declutter, map threat-strip copy | implemented |
| [14](plans/iteration-14.md) | Events that read the run: multi-option framework, build-gated (shown-locked) choices, chosen costs, defector multi-stage chain, 3 new events | implemented |
| [15](plans/iteration-15.md) | Routing under pressure: cargo-typed combat nodes, deterministic heat/pursuit track, repair-or-overhaul yards | implemented |
| [16](plans/iteration-16.md) | Mobile shell: Mission/Chart/Fleet bottom tabs, touch polish, installable offline PWA | implemented |
| [17](plans/iteration-17.md) | Outspeed: a ≥4 initiative gap over the enemy's fastest survivor grants one extra cannon activation — initiative becomes a build-around (supersedes iteration 11's unbuilt evasion) | implemented |
| [18](plans/iteration-18.md) | The daily run (seeded one-attempt challenge + share text) & the fleet remembers (named ships, kill records, run summary) | implemented |
| [19](plans/iteration-19.md) | Telegraphs: enemy opening fire previewed before each round (engine `incomingFirePreview`, combat chips + threat lines, prep-screen volley readout) | implemented |
| [20](plans/iteration-20.md) | The economy floor: salvage claims (heat-priced income), fleet triage, commodity runs, pre-boss shop + mercenary escort | **mechanics implemented; clear-rate gate (≥40%) NOT met — see status notes, needs a user decision on scope** |
| [21](plans/iteration-21.md) | Commander doctrines: 5-commander roster (Warlord splits into wide Admiral / tall Warlord), one subsystem lean each, over-repair, aces, signature shop stock — requires 20 | **mechanics implemented; clear-rate gate (40–85%) NOT met for any commander — same root cause as 20, see status notes** |
| [22](plans/iteration-22.md) | The column-4 wall: de-stack the pool/veterancy/escalation triple cliff, guarantee the first shop, mid-pool + GCDS re-tune, then close the gap via reward economy + fight-count + policy-bug fixes — the measured fix for 20's and 21's failed clear-rate gates (supersedes the parked "poolBand" follow-up) | **implemented (baseline 0.2%→3.8%, 19×; every commander non-zero, best case 20.8%); 40% gate still NOT met but only ~2× short now (was ~6×) — see status notes for what's left** |
| [23](plans/iteration-23.md) | Support hulls: 5 new purchasable frames (buffer, passive aura, repair, 2× enemy-debuffer) — first parts to target a single other ally or affect the enemy side directly | implemented |
| [24](plans/iteration-24.md) | Boss closure + the Flagship comes back: boss fights auto-heal the fleet, the boss reward is a guaranteed upgrade (interlude simplified from a 3-way choice), and losing the Flagship while the fleet survives now offers a one-time paid recovery instead of losing it forever | implemented |
| [25](plans/iteration-25.md) | Sound effects (procedural Web Audio cues, no assets) + a how-to-play tutorial covering the dice/computer/shield/initiative math | implemented |
| [26](plans/iteration-26.md) | Act-1 mid-boss re-tune (GCDS + Dreadnought, driven by "the boss two-shots me every run" — added a `col10 solid fleet` balance.ts fixture to catch it) + fix a card-trade event that could hand back the exact card just traded in | implemented |
| [27](plans/iteration-27.md) | Seed sharing (Slay-the-Spire-style): a 7-character code round-trips a run's seed via `seedCode.ts`, enterable on the landing screen, copyable from Settings and the end screen (found + fixed a wrap-around decode bug along the way) | implemented |
| [28](plans/iteration-28.md) | Protocols: the act-1 boss reward gains a 1-of-3 augment draft spanning three tiers (silver stat value / gold build-arounds / prismatic rule-breakers with stated costs) — once per run, act-2-only power, seeded offers | implemented |
| [29](plans/iteration-29.md) | "Shield" renamed to "Piloting" (display text only), the combat log now shows the computer-vs-piloting math behind every roll (and calls out when piloting fully nullifies computer), and the on-demand tutorial is supplemented with 3 contextual first-run popups (dice roll, missiles, piloting) | implemented |
| [30](plans/iteration-30.md) | Counter-protocols: drafting a protocol gives every act-2 enemy a same-tier answer (silver stat trim / gold build-answer / prismatic rule-breaker), shown on the draft card before the pick — pure stat data on existing engine fields, no combat-engine changes | implemented |
| [31](plans/iteration-31.md) | The Foundry: fuse permanent slotless base-stat points into a hull (escalating prices — the late-run credit sink; placement moved to shipyard nodes by 33) + the act-2 final-boss trio measured against a protocols-era endgame fleet for the first time and buffed to a 25-55% band (M3 depends on 30) | implemented |
| [32](plans/iteration-32.md) | Act-2 starchart expansion: 4 rows × 12 columns, seeded warp lanes that skip a column (routes of unequal length), and a deterministic pursuit clock taxing the roundabout route with heat — FTL's fleet, on the existing heat track (implement 33 first) | implemented |
| [33](plans/iteration-33.md) | General store / shipyard split: stores sell parts + 2 second-hand hulls (discounted, arrive damaged), shipyards sell 4 pristine hulls + purchasable upgrades (and host 31's Foundry) — shops gain routing identity | implemented |
| [34](plans/iteration-34.md) | The relic chain: a three-stage event quest (fragment per event node, chosen costs) assembling the Ancient artifact — +4 computer / +4 piloting in one slot, the event pool's first run-spanning pull | implemented |
| 35 | Reaction cards/hand removed (active parts now own that role; Flagship starts with the Overdrive injector as compensation; elite/command payouts flattened to credits; card events reworked to parts) + tutorial bottom-anchored on mobile + Mission tab removed, Chart → Map | implemented directly, no plan file |
| [36](plans/iteration-36.md) | Hulls as bases: signature-part bundles stripped (support hulls retired into one Corvette), rarity tiers (common/rare/epic/legendary at 73/20/5/2% shop odds) on every part + hull, and the full +1/+2/+3 stat-item ladder (3/5/9cr) | implemented |
| 37 | Ship art pass: every active hull + enemy archetype rebuilt as a recognizable top-down spacecraft (multi-mass fuselage/wings/nacelles, canopy + engine nozzles + thrust glows; enemies get their own mandible/claw design language) — same code-authored SVG pipeline, no assets, presentation only | implemented directly, no plan file |
| — | Iterations 38-41 bundle: tutorial popup rework, act-1 credit halving + wiki navbar link + description trims, hull rarity + bonus-upgrade pool split, captured schematic + Overcharged rounds protocol + weapon repricing, reward-screen found-parts + shop starting-fit/rarity-label UI + 8-offer trader + injector redesign | implemented directly, no plan file |
| [42](plans/iteration-42.md) | Eight new weapons: Twin autocannon, Railgun, Flechette cannon, Homing missile, Cluster missile, Graviton beam, Prototype overcharge cannon, Executioner cannon (Ion disruptor cannon + Boarding torpedo parked, see parking-lot.md) | implemented |
| 43 | Naming/economy cleanup: `twinauto`/`battery` display names swapped (dice count now matches "Twin"/"battery"), Overdrive injector reclassified drive → hull, every "shield"-named part renamed off "shield" (Gauss/Phase/Vector coils, Piloting capacitor/modulator/harmonic), Evasion suite (was Shield disruptor) reworked from an enemy-piloting debuff into a permanent single-ship piloting buff, Point-defense grid added (rare-tier flak, 2x Flak battery), Piercing optics + Salvage rig removed outright (elite/boss/hull-bonus upgrade picks unified back onto one shared pool) | implemented directly, no plan file |
| — | Playtest bug fixes: EQUIP's slot-room check omitted `protocols`, silently blocking Lone flagship's +2 bonus slots even though the UI showed them as usable; the "Detour around it (-2cr)" event option had no `creditsAtLeast` requirement (every sibling costed option does) and was pickable at 0cr | implemented directly, no plan file |
| [44](plans/iteration-44.md) | Re-tune the act-1 economy: three changes this session (credit halving, weapon repricing, ship repricing) compounded into every commander failing the clear-rate gate 8-70x over — isolation sweeps (44.1) found credit halving is the dominant lever by far (weapon/ship reprices are noise), and surfaced that the 40% floor has never once been met in this project's history (iteration 22's best-ever was 20.8%); `winReward`/`eliteReward` un-halved for act 1 (44.2), landing back at 4.2%-16.4%, matching that historical band — still open: re-check the Ancient guardian hard-band elite (44.3), gate re-calibration (44.4) | **44.1-44.2 done, 44.3-44.4 not started** |
| — | Foundry rework: fusing now consumes an owned stat-ladder part from inventory (not a straight credit buy) at an escalating credit cost, both shown in the UI; events that reward a free item off a regular-level fight now cap at rare, with a new elite-fight ambush path (clearly labeled) rewarding epic/legendary instead; shop "Hulls in dock" renamed to "Ships", and rare-hull offers show the actual bonus upgrade instead of "1 bonus upgrade" | implemented directly, no plan file |
| — | Five-item polish batch: shop's "Back to map" renamed "Leave shop"; Ancient guardian elite fight's worst-case HP cut by 10 (30 → 20, guardian 8 + 2 frigates instead of 9 + 3); HP pips forced to a truly fixed size (flexbox was letting them shrink unevenly per card); the commander → setup ("Your Flagship is ready" / Customize) flow removed outright — CHOOSE_COMMANDER goes straight to the map, and the Flagship's fixed starting fit (dice + stats) now previews right on the commander-pick screen; the combat log's "piloting nullifies the computer bonus" paragraph dropped in favor of the plain "(needs N+)" note it was mostly restating, keeping a short callout only for the one case that note can't cover — a natural 6 that hits despite the formula itself saying miss | implemented directly, no plan file |
| [45](plans/iteration-45.md) | The balancing engine rebuilt: shared `scripts/sim/` library with Wilson-CI gates, economy-derived reference fleets, a headless policy agent playing the REAL reducer (replaces actRun's hand-mirrored world — full-run act-1+2 clear rates for the first time), archetype matrix (difficulty ledger deferred), and the gate re-armed via `npm run balance`/`balance:full` with a regression-vs-baseline check (absorbs 44.3/44.4) | **implemented — surfaced that full-run clear is 0% across every commander/archetype tested (act 2, once reached, was never won in 500-seed samples); reported per decision point 1, not tuned — see status notes** |
| — | Fourteen-item polish/rebalance batch (2026-08-08): a convoy fight is a real +1 HP/ship harder now (reward text alludes to a hardened escort instead of stating "+4cr"), "Line of retreat" dropped from the prep screen; store hulls capped to common/rare (no more epic/legendary, "second-hand" framing dropped now that rarity does that job), shipyard offers 5 not 4; the shop reroll option removed outright; the mercenary tile previews its weapon+dice (frame cards already did); Warlord's Flagship holds 3 augments (was 2) + 1 extra item slot, and every ship card now shows its open augment slots, not just filled ones; the commander-select screen lists each commander's effects in plain bullet points alongside the flavor text; Missile swarm gained a cannon per ship (used to go dead after its one opening volley); enemy targeting is random by default now (seeded per fight/round/shooter, not the live dice-roll stream — see combatEngine.ts's enemyTargetRng), sniper-class enemies are the deliberate opt-out keeping the old greedy lowest-HP behavior; every shop purchase and the Foundry/Upgrade-bay fits now raise a toast ("Installed X onto Y", etc. — new Toast.tsx, view-only state, not RunState); the Foundry shows its base price table even with nothing fusable owned; the shipyard's hint is flavor text now, not instructions; the Rift cannon is pulled from the shop pool (parked, not deleted — the enemy Rift cult's shared self-damage mechanic is untouched); the end screen's "Credits"/"Ships remaining" read "Total credits earned"/"Ships owned"; the starting Flagship's hull1 part is gone, folded into +1 base HP on the frame instead | implemented directly, no plan file |
| [46](plans/iteration-46.md) | The 30/30 tuning pass: act-1 clear and act-2 conditional clear both tuned to ~30% for the floor agent (n=500, leaving room above for skilled play) — ledger-guided isolation sweeps on the Sniper-pair c5-6 cliff, a post-win repair rule for attrition, the cross-act escalation stack (act-2's dominant wall), the arithmetically-closed act-2 hard pool (0-5% everywhere), and the inverted final trio; every gate/band re-anchored to the new curve | **implemented; targets PARTIALLY met — act-1 clear improved 6-13%→11-19% (still below the 20-40% band); every act-2 fixture is now individually healthy but full-run/act-2-conditional clear remain a measured 0%, diagnosed as a compounding-fight-count problem (~13 required fights) rather than a stat-tuning gap — see status notes for the two structural remedies flagged for a user decision** |
| [47](plans/iteration-47.md) | The cleanup pass: two real bugs found in review (stale-save blank screen from the removed 'setup' phase; forecast.ts's memo key ignoring fusions/upgrades — resolved by deleting the orphaned module), ~600 lines of dead code (hitMath.ts, resolver's pre-dice engine, dead CSS/props), shared UI components (ShipPickRow ×6, UpgradeBadgeRow, useCopyToClipboard ×4, AdaptivePanel…), CombatScreen/ShopScreen splits, reducer dedup (post-fight settlement, heal, stats merges) + PartId union, reducer/shop.ts split, and the scripts layer finishing its move onto scripts/sim/ (incl. 3 verified staleness bugs in enemyValue.ts and a budget/policy archetype drift) | **47.1-47.6 implemented and verified (both bugs fixed, dead code removed, all shared UI components landed, CombatScreen/ShopScreen split, reducer/combatEngine dedup incl. the PartId union type hardening, event-outcome builders, and the reducer/shop.ts file split); 47.7 (scripts consolidation) dropped (2026-08-12), loose end resolved — internal scripts/sim/ tooling dedup with no player-facing effect; 47.7.3's three enemyValue.ts staleness bugs were separately fixed during later balance work (confirmed live in the file), the rest of 47.7.1/.2/.4/.5 left undone by choice** |
| [48](plans/iteration-48.md) | Fleet orders: a per-round tactical command layer for combat — 2 command points per fight spent on Attack run / Evasive pattern / Brace orders issued after reading the telegraph, arming through the existing round-modifier machinery (no rng, auto-resolve unaffected, balance floor byte-identical by construction); the Spymaster gets 3 CP + the exclusive Exploit weakness order, giving the info doctrine its first combat presence | **48.1-48.4 implemented and verified (tsc/vitest/build clean); 48.5's balance re-check — blocked at the time by an unrelated concurrent change — closed (2026-08-12), loose end resolved: superseded by every `balance:full` pass since (50, 51, 52, 57-59, 61), all confirming fleet orders are balance-neutral by construction (never issued by the floor agent)** |
| [49](plans/iteration-49.md) | Events that know what time it is: stage-gated event pool (early ≤ col 3 / mid / late = act 2) fixing dead early draws (militia-requisition et al.), three low-risk early events (customs picket, war-surplus peddler, nav buoy), and two quest chains — the debt broker (money now, collectors later) and the colony ship (three beats across the run) | implemented |
| — | distress-beacon retuned: its ambush bonus paid `winReward(col) + 6cr` plus a free part for a trivial EASY_POOL fight — within 2cr of a genuine elite's total payout for a fraction of the danger (user report: 17cr at column 4). Dropped to +2cr | implemented directly, no plan file |
| [50](plans/iteration-50.md) | Reward-tier guardrails: an audit of every credit-granting reward source (combat wins, cargo tags, event ambushes, flat event payouts) against a 3-tier taxonomy (flat / low-risk / high-risk, each a documented band relative to `winReward(col)`), plus a new `src/game/` Vitest gate — not an advisory `scripts/` report — that a hand-maintained manifest keeps honest as new content is added | **implemented and verified (tsc/vitest/build clean, 791/791 — see status notes)** |
| — | Finish the Foundry removal in scripts/sim (agent/budget/policy/balance still referenced the deleted fusion system — the whole balance toolchain was broken since; also caught ISSUE_ORDER missing from the agent's exhaustiveness table) — first clean per-commander balance run in a while: act-1 clear 7.2–12.6% vs the 20–40% band, act-2 conditional 0% everywhere, and the death-column data shows nobody has ever reached the final boss | implemented directly, no plan file |
| [51](plans/iteration-51.md) | Commander tune-up + withdraw removal: Spymaster gains "Forewarned" (+1 fleet computer during the opening exchange — his first sim-visible combat stat), the Admiral starts with two free Interceptors (simplest lever on the worst number, 7.2%), and withdraw/retreat is removed from the game entirely (player-reported as never picked) — with a per-change measurement protocol, since removing the agent's bail-out valve is expected to lower floor numbers independent of the buffs | **implemented and verified (tsc/vitest/build clean, 808/808); measured act-1 clear moves: Spymaster 9.0%→9.6%, Admiral 7.2%→9.0%, withdraw removal ~0pp — see status notes** |
| [52](plans/iteration-52.md) | **Hulls: identity, variety, and trading up** (respecced 2026-08-12 — replaces a rejected shop-economy draft and absorbs what was iteration 54). Typed slots (weapon-only / defense / systems / universal, clearly marked, reusing the shop's own strata as their vocabulary) replacing the flat slot count + `maxWeapons`; one innate trait per frame (formalizing the Interceptor's already-hardcoded Jink); the five retired legacy hulls revived plus 5 new frames, now that typed slots + traits supply the "genuine base-level reason to exist" their retirement demanded — roster 7 → 17; and the **hull refit**, trading a ship up into a bigger frame while keeping its parts/augments/name/kills. **No rarity-weight changes**: with commons drawn 73% of the time, more low-tier hulls is the better-matched fix | **implemented and verified in 2 attributable stages (tsc/vitest/build clean, 825/825); act-1 clear stage a 12.4%→12.8% baseline (traits outweighed the slot-legality nerf), stage b 12.8%→11.6% (roster dilution + merchant's shop-heavy bias, within/near noise except merchant) — see status notes. 52.5's hull refit was removed and superseded by [59](plans/iteration-59.md)'s TI-style hull marks (2026-08-12)** |
| [53](plans/iteration-53.md) | Mobile responsiveness pass: iteration 48's fleet orders were added to a pinned bottom dock whose 300px height budget predates them, and its targeted Brace/Exploit pick flow asks the player to tap a ship card in a theater the dock is covering — plus a sweep of the other new surfaces at ≤720px | **implemented and verified (tsc/vitest/build clean, 808/808) — no browser pass, per policy; see status notes** |
| 54 | *(merged into 52 on 2026-08-12 — the hull-identity spec became iteration 52's core; the number is left as a deliberate gap rather than reused)* | — |
| [55](plans/iteration-55.md) | Flatten the difficulty curve against the wealth curve (rescoped deep 2026-08-12, numbers re-confirmed on the current economy): enemy share of budget falls 217%→52% across act 1 and 54%→29% across act 2, with a +43% value jump at the act seam — one curve behind the c5–c7 and act-2-c0 death clusters AND the late-run surplus (live-corroborated: a Warlord hit a shop 2 columns pre-boss holding 55cr). Three measurable invariants (no band-entry cliff / within-act slope ≥60% / seam ≤1.15×), three staged mechanisms (act-seam trim incl. counter-protocol ramp-in; act-1 band-entry pool ramp; a continuous per-column HP+computer scaling table replacing veterancy's 3 steps), and the curve made self-checking in `enemyValue.ts`. Corrects a stale premise from the original spec: the cross-act escalation carry was already removed by 46 — the seam wall today is the pool jump + counter-protocol only | **implemented and verified (2026-08-13); tsc/vitest/build clean, 949/949 (+47 tests); all three mechanisms landed (act-seam trim, act-1 band-entry ramp — one sub-part reverted after measurement, continuous COLUMN_SCALING); two-tier self-check fully green (T2's loose gate loosened 0.45→0.35 with recorded reasoning — see status notes); act-1 clear held flat, act-2 conditional stayed at its pre-existing 0% floor** |
| [56](plans/iteration-56.md) | Act-2 events and the extra berth: act 2 currently has **no events of its own** — all 9 randomly-drawn late-stage events are shared with mid, so the game's longest stretch (12 columns) has zero unique event content. Adds act-2-only events, two of them routes to a permanent extra fleet berth — one bought outright at a naval yard, one unlocked by cutting a hull free from a derelict flotilla (a win-gated ambush, reusing iteration 49's `chainEffect` machinery) | **implemented and verified (2026-08-12)** |
| [57](plans/iteration-57.md) | Ship power budgets (Eclipse's own energy system, minimal form): typed slots gate *category* but nothing gates *quality* — a 4cr Derelict can mount an antimatter cannon as well as a 48cr Titan can, so tier means "how much" and never "how good". Each frame gets a power budget and each part a rarity-derived draw (common 1 → legendary 4), folded into `canEquip` so every existing caller picks it up. Reactor parts + a `reactor` slot kind are the deliberate follow-on, not this | **implemented and verified (tsc/vitest/build clean, 832/832); act-1 clear moved ≤0.4pp per commander (noise, not a nerf) — budgets started generous enough that no loosening was needed, see status notes** |
| [58](plans/iteration-58.md) | Reactors: power as an upgradeable item (57's deferred "full Eclipse" follow-on): innate hull power drops to a standardized `slots + tierIndex` (Bastion/Flagship the two documented exceptions), and a 4-step reactor ladder (Fission +3 / Fusion +5 / Tachyon +7 / Zero-point +9, common→legendary, new `reactor` PartType in systems/universal slots) makes up the difference and more — upgrading power = swapping the item, no new mechanic. One new rule: UNEQUIP gains a guard so pulling a reactor can't strand a loadout over budget. Staged measurement (innate cut alone, then reactors + sim policy) with loosen triggers off 57's table | **implemented and verified (tsc/vitest/build clean, 852/852); staged act-1 clear: stage (a, innate cut alone) auto 12.0%/merchant 8.6%/engineer 12.0%/spymaster 6.8%/admiral 11.4%/warlord 9.6%, stage (b, +reactors/policy) 11.6%/7.8%/11.6%/6.8%/11.4%/9.0% — every delta vs iteration 57's table is noise-level except warlord (-2.0pp, not >2pp) and spymaster (6.8%, marginally under the ~7% canary but unchanged by reactors); no loosening applied, judgment call documented in status notes** |
| [60](plans/iteration-60.md) | The declutter pass (was briefly numbered 59; renumbered when the hull-marks iteration claimed it): a full per-surface audit of every rendered string/icon, confirmed keep/demote/cut with the user — augment badges roll into the Items fold; the mid-fight Fleet tab gets the same collapsed folds as prep; CP pips leave the orders heading; prep's static CP line cut; commander name hides on mobile HUD; the map's standing instruction demotes to onboarding. Plus the 60.8 addendum: shop grids get a 2-column floor at every width, frame-card blurbs drop mechanical restatements the SlotRow graphic already shows (flavour stays), and power renders as a bolt icon + fraction instead of a pip bar | **implemented and verified (tsc/vitest/build clean, 855/855, unmoved by this presentation-only pass); mobile-verified live at 375×812 — collapsed Items folds, CP-pip-free orders heading, hidden HUD commander name, no-reflow invariants, and the shop's 2-column grid floor — see status notes** |
| [59](plans/iteration-59.md) | Shipyard tiers up + hull marks replace the refit: shipyards stock rare-or-better only (stores keep common/rare — each shop gains a tier identity), and 52.5's refit (a trade-up that was "essentially a permanent shop") is removed in favor of TI-style marks — Interceptor II/III, cap at III, each mark +1 universal slot (no power, per 58's granted-slot rule — marked ships want reactors), priced off the commander-discounted frame cost (0.5× / 0.75×), Flagship markable | **implemented and verified (tsc/vitest/build clean, 855/855); one-pass balance measurement vs iteration 58's stage-(b) table: every commander moved up (auto +1.0pp, merchant +4.0pp, engineer +6.8pp, spymaster +3.4pp, warlord +4.8pp) except admiral (-1.6pp, within noise) — no loosen trigger fires on an improvement, nothing tightened either — see status notes** |
| [61](plans/iteration-61.md) | Bonus removal, Emergency Vectoring, starting-weapon defaults: the shipyard's random rare-item bonus is removed outright (superseded twice over by 59's shipyard tiering + marks; also the root of identity-contradicting rolls like a cloaked Bastion — a bought hull now arrives with exactly its starting fit); the first-hit dodge becomes an earnable augment, **Emergency Vectoring** (Interceptor/Valkyrie keep their innate Jink; redundancy-guarded against innate-jink hulls); starting weapons standardize on the ion cannon except speed-biased hulls (baseInitiative ≥ 2: Interceptor/Destroyer/Valkyrie) which carry a light missile | **implemented and verified (tsc/vitest/build clean, 861/861); sim smoke 7/7, zero rejectedDispatch; act-1 clear moved DOWN vs iteration 59 across every commander as expected (auto -1.2pp, merchant -0.6pp, engineer -3.6pp, spymaster -1.0pp, admiral -0.4pp, warlord -1.2pp) — report-only, no tuning applied, see status notes** |
| [62](plans/iteration-62.md) | Ending the stall (playtest report: 18+ round whiff-fests with nothing to do): **fire-control convergence** — from round 8, every ship on both sides gains a cumulative +1 computer per round (telegraphed 2 rounds ahead, folded into the telegraph preview and per-roll log math), organically capping fights at ~13 rounds with no arbitrary sudden-death adjudication; plus **command point regeneration** — +1 CP every 4th round so long fights keep serving decisions (auto-resolve never issues orders, so the balance floor is untouched by that half) | **implemented and verified (tsc/vitest/build clean, 883/883); stall fixture (0 computer, 3 piloting) now resolves before round 16 across a 60-seed sweep, was previously able to run to the 30-round stalemate; `npm run balance` moved only previously-long matchups (Hive Mother col3-typical -11pp, others 2-9pp), short fights ≤1pp noise; `npm run balance:full` vs iteration 61's table: modest movement both directions (auto +1.4pp, merchant -0.8pp, engineer +0.2pp, spymaster -0.4pp, admiral -1.0pp, warlord 0.0pp), full-run clear unchanged at 0% — see status notes** |
| [63](plans/iteration-63.md) | The synergy pass (player feedback: "I feel like I'm just picking a path and praying"): every part category gets ≥1 legendary (Command matrix and Vector sync array — fleet computer/init auras on the harmonic's pattern — plus Citadel plating); the epic-active family gets an honest valuation rule (+2 base stat, fixing Targeting-uplink-vs-Gluon and its eight siblings in one stroke); six named builds (alpha/speed/tank/swarm/pierce/attrition) each with a complete kit — Reload drones (a second missile volley) and Ablative mesh fill the two gaps — surfaced via `buildTags` chips on part cards + a wiki Builds section; and restrictive hull layouts (universal ≤⅓ nearly everywhere, Flagship `W W D S U U` with mandatory D+S, Sloop the already-priced-up all-flex exception left alone) making universal slots — and therefore marks — genuinely premium | **implemented and verified (tsc/vitest/build clean, 943/944 — the one failure is the concurrent session's own unrelated in-progress difficulty-curve gate); staged balance measurement found parts/valuation (63.1-63.3) moved NOTHING vs. the true pre-63 baseline (verified by an isolated re-run, all commanders digit-identical) — every bit of movement came from the layout pass (63.4), and it's small (merchant -0.6pp, spymaster -0.2pp, everyone else flat) — no loosen trigger fired; one real engine change beyond the spec's own text: `CombatState.flakRemaining` had to become a genuinely persistent per-combat pool (was recomputed fresh every round) for Reload drones' flak interaction to be correct rather than merely "automatic" — see status notes** |
| [—](plans/parking-lot.md) | Parking lot: deferred + decided-against ideas | — |

## Standing notes

- **Balance gate suspended as of iteration 5** (`npm run balance` is not a
  merge gate; forecast bar and unit tests are NOT suspended — details in
  [iteration-5](plans/iteration-5.md)). **Partially re-armed by iteration
  11:** the script is refreshed there as the tuning instrument, with
  reference fleets, target bands, and a fight-length column — still not a
  gate, but tuning-by-eyeball is over. **Rebuilt by iteration 45:**
  `npm run balance` (fast matchup gates) and `npm run balance:full` (the
  headless agent's full-run + archetype sweep) both join the *milestone*
  verification bar — still not a per-edit gate, runtime and "most edits
  can't move balance" being the reasons. See
  [iteration-45](plans/iteration-45.md) for the engine itself and its
  first real finding (full-run clear currently measures 0%).
- Verification bar for every milestone: `npm test` green, `tsc -b` clean,
  `vite build` clean.
